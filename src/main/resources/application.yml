spring:
  datasource:
    url: jdbc:mysql://localhost:3306/backendestopenda
    username: estudo
    password: Estudo123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false